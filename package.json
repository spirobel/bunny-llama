{
  "name": "bunny-llama",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "clean-llama": "rm -rf llama.cpp && echo 'Done.'",
    "clean-bunny": "rm -rf build && echo 'Done.'",
    "format": "bunx biome format . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.2.2",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}