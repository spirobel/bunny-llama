{
	"name": "bunny-llama",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"clone": "bun buntils/clone_llama.ts",
		"make": "cd llama.cpp && make -j libllama.so",
		"make-cuda": "bun buntils/make_cuda_llama.ts",
		"ride": "bun ride.ts",
		"clean-llama-repo": "rm -rf llama.cpp && echo 'Done.'",
		"clean": "cd llama.cpp && make clean && echo 'Done.'",
		"format": "bunx biome format . --write"
	},
	"devDependencies": {
		"@biomejs/biome": "1.2.2",
		"bun-types": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}